<snippet>
	<content><![CDATA[
frappe.db.get_value("${1:DocType}", {"field_name": ${2:filter}}, "${3:select_field}", (r) => {
	var result = r.${3:select_field}
});
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>frappe.db.get_value</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.js</scope>
</snippet>
